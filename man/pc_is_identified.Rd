% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_rank.R
\name{pc_is_identified}
\alias{pc_is_identified}
\title{Is a linear functional of the conditioning path identified by a design?}
\usage{
pc_is_identified(design, lambda, tol = 1e-08)
}
\arguments{
\item{design}{an object returned by \code{\link[=pc_design_rank]{pc_design_rank()}}.}

\item{lambda}{numeric vector of weights, one per observed tenure (in the order
of \code{design$tenures}), defining the functional \eqn{\sum_s \lambda_s \tau(s)}.}

\item{tol}{tolerance.}
}
\value{
\code{TRUE} if the functional annihilates every direction of the identified
set (hence is point identified from cell means), \code{FALSE} otherwise.
}
\description{
Is a linear functional of the conditioning path identified by a design?
}
\examples{
d <- pc_design_rank(c(1, 5, 13), 19)
lam <- numeric(19); lam[c(2, 3, 4)] <- c(1, -2, 1); pc_is_identified(d, lam)   # D^2 tau(3): FALSE
lam <- numeric(19); lam[c(1, 5, 9)] <- c(1, -2, 1); pc_is_identified(d, lam)   # D_4^2 tau(5): TRUE
}
